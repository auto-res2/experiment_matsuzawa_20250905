global:
  seeds: [0, 1, 2]
  device: "cuda"
  hidden_dim: 64
  mixed_precision: true
  epochs: 1000
  early_stop_patience: 100
  lr: 0.005
  weight_decay: 0.0005
  dropout: 0.5

experiment_1:
  name: "Depth-Scaling Benchmark"
  datasets: ["Cora", "Citeseer", "PubMed", "Chameleon", "Squirrel", "Texas"]
  depths: [2, 8, 16, 32, 64, 128]
  models: ["GCN", "DropEdge", "GCNII", "CURVONet"]
