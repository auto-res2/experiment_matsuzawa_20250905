[project]
name = "gcid"
version = "0.1.0"
description = "GCID – Generative Causal Interventions with Diffusion"
requires-python = ">=3.10"
readme = "README.md"

dependencies = [
    "torch",
    "torchvision",
    "wilds",
    "timm",
    "faiss-gpu",
    "torchcam",
    "diffusers",
    "transformers",
    "matplotlib",
    "seaborn",
    "PyYAML",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"